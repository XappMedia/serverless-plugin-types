{
  "name": "@xapp/serverless-plugin-type-definitions",
  "version": "0.1.23",
  "description": "Common typescript types that can be used for creating serverless plugins.",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf ./node_modules",
    "version:release": "standard-version --message '[skip ci] v%s'",
    "postversion:release": "git push --follow-tags origin master && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/XappMedia/serverless-plugin-types.git"
  },
  "keywords": [
    "serverless",
    "plugin"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "standard-version": "^4.4.0"
  }
}
